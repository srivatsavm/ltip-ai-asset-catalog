apiVersion: aia.aiassets/v1
kind: McpTool
metadata:
  name: claims-getassessmenthistory
  displayName: Claims.GetAssessmentHistory
  domain: Claims
  version: 1.0.0
  generatedAt: '2026-07-03T21:18:07.107737+00:00'
spec:
  owningMcpServer: claims-mcp-server
  mode: ReadOrchestration
  description: Assessment API + Decision Letter API + Document API
  allowedSkills:
  - Claims.EligibilityAndLiabilityValidation
  inputData: claimId; includeDocuments flag; dateRange optional
  outputData: 'assessments: assessmentId; assessmentDate; assessmentType; decision; rationale; assessor; relatedDecisionLetters;
    relatedDocumentIds; sourceReference'
  backendApiMappings:
  - apiName: Assessment API + Decision Letter API + Document API
    candidateEndpointOrOperation: GET /claims/{claimId}/assessments; GET /claims/{claimId}/decision-letters; GET /documents?claimId={claimId}&type=assessment
    mappingPattern: multi-api orchestration in MCP tool handler
  handler:
    recommendedRuntime: Azure Function handler or Container App route
    orchestrationResponsibilities:
    - validate input
    - call backend API/API set through APIM
    - normalise response
    - attach source references
    - emit audit event
  controls:
  - validateInputSchema
  - validateCallerAgent
  - validateInvokingSkill
  - validatePurpose
  - emitAuditStartAndEnd
  - returnEvidenceReferences
  - structuredErrors
  errorModel:
  - ValidationError
  - AccessDenied
  - BackendUnavailable
  - PartialResponse
  - NotFound
  - ConflictingSourceData
  - HumanReviewRequired
